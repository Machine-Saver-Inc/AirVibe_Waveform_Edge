# AirVibe_Waveform_Edge
Edge runtime (API + MQTT broker + infra) for AirVibe Time Waveform ingestion. Runs on a VPS behind edge.&lt;domain> with Let’s Encrypt server TLS and private-CA client mTLS for Actility. SQLite + local disk in Phase 1. Includes GitHub Actions for DNS, Cloudflare Access, server deploy, and issuing Actility connector certificate bundles.
