# Domain config
DOMAIN=machinesaver.com

# API
API_PORT=8080
UI_ORIGIN=https://ui.machinesaver.com

# Admin token to authorize PKI endpoints (/admin/pki/*)
# Set a strong random value; used by your UI setup page or curl.
ADMIN_TOKEN=A1B2C33C2B1A

# Certificates mode for Mosquitto server:
# - private      (default; generates a private Server CA and server cert)
# - letsencrypt  (requires CF_API_TOKEN to be provided to cloud-init)
CERT_MODE=private

# Storage (Phase 1: local)
SQLITE_PATH=/app/data/twab.sqlite
WAVEFORM_DIR=/app/data/waveforms
SEGMENT_DIR=/app/data/segments
